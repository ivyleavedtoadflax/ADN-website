[project]
name = "adn-scraper"
version = "0.1.0"
description = "Polite, GET-only scraper for adn.gob.do construction-permit content"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "selectolax>=0.3"]
